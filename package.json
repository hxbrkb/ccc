{
    "name": "character-rating-system",
    "version": "1.0.0",
    "description": "角色评级管理系统",
    "main": "server/server.js",
    "scripts": {
        "start": "node server/server.js",
        "dev": "nodemon server/server.js",
        "client": "cd client && live-server --port=3001",
        "install-all": "npm install && cd client && npm install"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^16.5.0",
        "express": "^4.18.2",
        "multer": "^1.4.5-lts.1",
        "mysql2": "^3.2.0",
        "xlsx": "^0.18.5"
    },
    "devDependencies": {
        "nodemon": "^2.0.20"
    }
}
